services:
  - type: web
    runtime: static
    name: pyxis-code
    repo: https://github.com/Stasshe/Pyxis-CodeCanvas
    buildCommand: pnpm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_IS_DEV_SERVER
        value: "true"
    previews:
      generation: automatic
